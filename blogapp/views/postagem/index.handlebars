<div class="card">
    <div class="card-body">
        <h1>{{post.titulo}}</h1>
        <hr>
        <small>{{#formatDate post.date }}{{/formatDate}}</small><br>
        <hr>
        <small>{{post.conteudo}}</small>
    </div>
</div>